{
	"name": "koyt-site",
	"homepage": "https://isar0se.github.io/koyt-site",
	"version": "0.1.0",
	"private": false,
	"devDependencies": {
		"gh-pages": "^0.12.0",
		"react-scripts": "0.9.5"
	},
	"dependencies": {
		"react": "^15.5.4",
		"react-dom": "^15.5.4"
	},
	"scripts": {
		"start": "react-scripts start",
		"build": "react-scripts build",
		"predeploy": "npm run build",
		"deploy": "gh-pages -d build",
		"test": "react-scripts test --env=jsdom",
		"eject": "react-scripts eject"
	}
}
